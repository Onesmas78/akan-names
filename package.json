{
  "name": "akan-names",
  "version": "1.0.0",
  "description": "Application to generate akan names based on dates",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
